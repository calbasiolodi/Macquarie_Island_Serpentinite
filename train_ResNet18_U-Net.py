"""These are imports of essential packages to train the PyTorch model that is saved to a .pth (path) file, that is re-usable and more importantly can be fine-tuned.
"""
import torch
import torch.nn as nn
